"use client"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export const ReportBuilder = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Report Builder</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Report builder interface will be displayed here.</p>
      </CardContent>
    </Card>
  )
}
